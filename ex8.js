/* Escre um programa JavaScript que concatene duas strings menos a primeira letra*/

const concatenaString = (str1, str2) => str1.slice(1) + str2.slice(1);

console.log(concatenaString("Rayara", "Chambela"));